# Azure Cost Monitoring Dependencies
# Install with: pip install -r requirements-cost-monitoring.txt

# Azure SDK packages for cost management
azure-mgmt-costmanagement>=4.0.0
azure-identity>=1.12.0
azure-mgmt-resource>=23.0.0

# HTTP requests for webhook notifications
requests>=2.28.0

# JSON processing (usually included with Python)
# jq equivalent for Python (optional, for advanced JSON processing)
jq>=1.4.0

# Additional utilities
python-dateutil>=2.8.0
